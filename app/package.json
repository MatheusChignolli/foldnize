{
  "name": "foldnize-app",
  "productName": "Foldnize",
  "version": "1.0.3",
  "private": true,
  "description": "Foldnize desktop app — Electron UI on top of the foldnize library.",
  "main": "dist/main.js",
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "build:main": "tsc -p tsconfig.json",
    "build:renderer": "tsc -p tsconfig.renderer.json",
    "build:assets": "node scripts/copy-assets.mjs",
    "build": "npm run clean && npm run build:main && npm run build:renderer && npm run build:assets",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.renderer.json --noEmit",
    "icons:build": "node scripts/build-icons.mjs",
    "dmg:build": "node scripts/build-dmg-assets.mjs",
    "pack": "npm run build && npm run icons:build && electron-builder --dir",
    "dist": "npm run build && npm run icons:build && electron-builder",
    "dist:mac": "npm run build && npm run icons:build && npm run dmg:build && electron-builder --mac",
    "dist:win": "npm run build && npm run icons:build && electron-builder --win",
    "dist:linux": "npm run build && npm run icons:build && electron-builder --linux",
    "start": "npm run build && electron .",
    "start:fast": "electron ."
  },
  "keywords": [
    "electron",
    "files",
    "organizer",
    "rename",
    "media"
  ],
  "author": "Matheus Chignolli",
  "license": "MIT",
  "homepage": "https://github.com/MatheusChignolli/foldnize/tree/main/app#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MatheusChignolli/foldnize.git",
    "directory": "app"
  },
  "bugs": {
    "url": "https://github.com/MatheusChignolli/foldnize/issues"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@types/node": "^25.9.1",
    "electron": "^42.3.0",
    "electron-builder": "^26.16.1",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "foldnize": "file:../library"
  },
  "optionalDependencies": {
    "exiftool-vendored.exe": "13.59.0"
  }
}
